const initialState = {
  user: null,
  loading: false,
  error: null,
  jwt: null,
  projectSize: 0,
};

const demoReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      state;
  }
};
export default demoReducer;
